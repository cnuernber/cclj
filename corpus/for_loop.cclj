;and dashing into the land of mutability

(defn slow-pow|f32 [val|f32 pow|u32]
	;for loops return const u32 0.
  (let [result val]
  	(for [idx 1|u32 end pow]
  		 (< idx end)
  		 [(set idx (+ idx 1|u32))]
  	  (set result (* result val)))
  	result))
		
(slow-pow 5|f32 3|u32)